using LinqToWql.Language;
using LinqToWql.Test.Mocks;

namespace LinqToWql.Test; 

public class WqlResourceTest {
  [Fact]
  public void Test() {
    //var resource = MakeMockResource();
    //
    //var q = resource
    //        .Where(c => c.Name == "Win32_Process")
    //        .Where(c => c.CollectionID.IsA("Win32_Process"))
    //        .Where(c => c.Description == "Process")
    //        .Select(c => c.Name);
//
    //q.GetEnumerator();
  }

}
